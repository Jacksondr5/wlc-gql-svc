type Challenge {
  name: String!
  startDate: String!
  endDate: String!
  participants: [User!]!
  totalPrizeMoney: Int!
  # weeklyWeighIns: [WeeklyWeighIn!]!
}

input NewChallenge {
  name: String!
  startDate: String!
  endDate: String!
  entryFee: Int!
}
